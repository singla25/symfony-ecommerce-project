{% extends 'base.html.twig' %}

{% block body %}
    <nav class="navbar navbar-expand-lg shadow-sm d-flex justify-content-between">
        <div class="container">
            <a class="navbar-brand" href="{{ path('home_page') }}">
                <img src="{{ asset('img/logo.png') }}" alt="Logo" height="40">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <li class="nav-item px-4">
                        <a class="nav-link fw-semibold fs-6" href="{{ path('shop_page') }}">Shop</a>
                    </li>
                    <li class="nav-item px-4">
                        <a class="nav-link fw-semibold fs-6" href="{{ path('blog_page') }}">Blog</a>
                    </li>
                    <li class="nav-item px-4">
                        <a class="nav-link fw-semibold fs-6" href="{{ path('about_page') }}">About</a>
                    </li>
                    <li class="nav-item px-4">
                        <a class="nav-link fw-semibold fs-6" href="{{ path('contact_page') }}">Contact</a>
                    </li>
                    <li class="nav-item px-4">
                        <a class="nav-link fw-semibold fs-6" href="{{ path('cart_page') }}">
                            <i class="fa-solid fa-bag-shopping"></i>
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}
